{% trans_default_domain 'FOSUserBundle' %}

	{{ form_start(form, {'method': 'post', 'action': path('fos_user_change_password'), 'style': 'horizontal', 'attr': {'class': 'fos_user_change_password', novalidate: 'novalidate'}}) }}
	    {{ form_row(form.current_password, { 'label': 'Current password', 'label_col': 2, 'widget_col': 2}) }}
	    {{ form_row(form.plainPassword.first, { 'label': 'New password', 'label_col': 2, 'widget_col': 2}) }}
		{{ form_row(form.plainPassword.second, { 'label': 'Repeat new password', 'label_col': 2, 'widget_col': 2}) }}
		{{ form_widget(form) }}
	{{ form_end(form) }}

